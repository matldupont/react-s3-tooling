import * as React from 'react';

import { StyledHeader, HeaderText } from './header.styled';

const Header = () => {
  return (
    <StyledHeader>
      <HeaderText>
        woodward bicycle co
      </HeaderText>
    </StyledHeader>
  );
};

export default Header;
