import * as React from 'react';

import {
  GallerySection,
  GalleryHeading,
} from './gallery.styled';

const Gallery = () => {
  return (
    <GallerySection>
      <GalleryHeading>
        Gallery
      </GalleryHeading>
    </GallerySection>
  );
};

export default Gallery;
